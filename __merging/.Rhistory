# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
same_variable_values <- data[species_index, same_variable_sources]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
same_variable_sources = same_variable_sources,
cell_value = cell_value,
same_variable_values = same_variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
intermediate_data <- combined_data
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
same_variable_columnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all sources for the same variable and species
same_variable_sources <- sapply(strsplit(same_variable_columnames, "__"), function(x) x[2])
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
same_variable_values <- data[species_index, same_variable_columnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
same_variable_sources = same_variable_sources,
cell_value = cell_value,
same_variable_values = same_variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
# ## ## #END SOMETHING
intermediate_data <- combined_data
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
same_variable_columnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all sources for the same variable and species
same_variable_sources <- sapply(strsplit(same_variable_columnames, "__"), function(x) x[2])
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
same_variable_values <- data[species_index, same_variable_columnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
same_variable_sources = same_variable_sources,
same_variable_columnames = same_variable_columnames,
cell_value = cell_value,
same_variable_values = same_variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
# Create an intermediate dataframe where lesser-ranked source values are marked as "WORSE."
intermediate_data <- combined_data
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
same_variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all sources for the same variable and species
same_variable_sources <- sapply(strsplit(same_variable_colnames, "__"), function(x) x[2])
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
same_variable_values <- data[species_index, same_variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
same_variable_sources = same_variable_sources,
same_variable_colnames = same_variable_colnames,
cell_value = cell_value,
same_variable_values = same_variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
# ## ## #END SOMETHIN
# Create an intermediate dataframe where lesser-ranked source values are marked as "WORSE."
intermediate_data <- combined_data
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
same_variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all sources for the same variable and species
same_variable_sources <- sapply(strsplit(same_variable_colnames, "__"), function(x) x[2])
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
same_variable_sources_valid <- same_variable_sources[!is.na(data[species_index, same_variable_columnames]) & data[species_index, same_variable_columnames] != "WORSE"]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
same_variable_values <- data[species_index, same_variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
same_variable_colnames = same_variable_colnames,
same_variable_sources = same_variable_sources,
same_variable_sources_valid = same_variable_sources_valid,
cell_value = cell_value,
same_variable_values = same_variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
same_variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all sources for the same variable and species
same_variable_sources <- sapply(strsplit(same_variable_colnames, "__"), function(x) x[2])
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
same_variable_sources_valid <- same_variable_sources[!is.na(data[species_index, same_variable_colnames]) & data[species_index, same_variable_columnames] != "WORSE"]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
same_variable_values <- data[species_index, same_variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
same_variable_colnames = same_variable_colnames,
same_variable_sources = same_variable_sources,
same_variable_sources_valid = same_variable_sources_valid,
cell_value = cell_value,
same_variable_values = same_variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
same_variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all sources for the same variable and species
same_variable_sources <- sapply(strsplit(same_variable_colnames, "__"), function(x) x[2])
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
same_variable_sources_valid <- same_variable_sources[!is.na(data[species_index, same_variable_colnames]) & data[species_index, same_variable_colnames] != "WORSE"]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
same_variable_values <- data[species_index, same_variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
same_variable_colnames = same_variable_colnames,
same_variable_sources = same_variable_sources,
same_variable_sources_valid = same_variable_sources_valid,
cell_value = cell_value,
same_variable_values = same_variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
# ## ## #END SOMETHING
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid2 <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all sources for the same variable and species
variable_sources <- sapply(strsplit(variable_colnames, "__"), function(x) x[2])
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- variable_sources[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid2 <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid, non-"NA" non-"WORSE" sources for the same variable and species
#variable_sourceranks_valid <- variable_sources[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources = variable_sources,
variable_sources_valid = variable_sources_valid,
variable_sources_valid2 = variable_sources_valid2,
cell_value = cell_value,
variable_values = variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
# ## ## #END SOMETHING
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid sources for the same variable and species
variable_sources_valid_ranks <- worth_dataframe$priority[match(same_variable_sources_valid, worth_dataframe$source)]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources_valid = variable_sources_valid,
cell_value = cell_value,
variable_values = variable_values,
variable_sources_valid_ranks = variable_sources_valid_ranks,
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid sources for the same variable and species
variable_sources_valid_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources_valid = variable_sources_valid,
cell_value = cell_value,
variable_values = variable_values,
variable_sources_valid_ranks = variable_sources_valid_ranks,
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
## 1.6.2 Limit dataset to best available data
# Create an intermediate dataframe where lesser-ranked source values are marked as "WORSE."
intermediate_data <- combined_data
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid sources for the same variable and species
variable_sources_valid_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources_valid = variable_sources_valid,
variable_sources_valid_ranks = variable_sources_valid_ranks,
cell_value = cell_value,
variable_values = variable_values,
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid sources for the same variable and species
variable_sources_valid_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources_valid = variable_sources_valid,
variable_sources_valid_ranks = variable_sources_valid_ranks,
cell_value = cell_value,
variable_values = variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
# ## ## #END SOMETHING
## 1.6.2 Limit dataset to best available data
# Create an intermediate dataframe where lesser-ranked source values are marked as "WORSE."
intermediate_data <- combined_data
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid sources for the same variable and species
variable_sources_valid_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]
# Extract the BEST rank for valid sources for the same variable and species
variable_sources_best_rank <- min(worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)])
# Extract all WORSE ranks for valid sources for the same variable and species
variable_sources_worse_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)] %>%
filter(. > min(worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]))
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources_valid = variable_sources_valid,
variable_sources_valid_ranks = variable_sources_valid_ranks,
variable_sources_best_rank = variable_sources_best_rank,
variable_sources_worse_ranks = variable_sources_worse_ranks,
cell_value = cell_value,
variable_values = variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid sources for the same variable and species
variable_sources_valid_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]
# Extract the BEST rank for valid sources for the same variable and species
variable_sources_best_rank <- min(worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)])
# Extract all WORSE ranks for valid sources for the same variable and species  # Filter for ranks greater than the minimum rank
variable_sources_worse_ranks <- variable_sources_valid_ranks[variable_sources_valid_ranks > min(worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)])]
# Extract all WORSE ranks for valid sources for the same variable and species
variable_sources_worse_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)] %>%
filter(. > min(worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]))
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources_valid = variable_sources_valid,
variable_sources_valid_ranks = variable_sources_valid_ranks,
variable_sources_best_rank = variable_sources_best_rank,
variable_sources_worse_ranks = variable_sources_worse_ranks,
cell_value = cell_value,
variable_values = variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
# ## ## #TRY SOMETHING
get_info_for_cell <- function(data, species_index, column_index) {
species <- data$Species[species_index]
column_name <- colnames(data)[column_index]
# Extract variable and source from the column name
variable_source <- strsplit(column_name, "__")[[1]]
variable <- variable_source[1]
source <- variable_source[2]
# Extract all column names with the same variable
variable_colnames <- grep(paste0("^", variable, "__"), colnames(data), value = TRUE)
# Extract all valid, non-"NA" non-"WORSE" sources for the same variable and species
variable_sources_valid <- (sapply(strsplit(variable_colnames, "__"), function(x) x[2]))[!is.na(data[species_index, variable_colnames]) & data[species_index, variable_colnames] != "WORSE"]
# Extract all ranks for valid sources for the same variable and species
variable_sources_valid_ranks <- worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)]
# Extract the BEST rank for valid sources for the same variable and species
variable_sources_best_rank <- min(worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)])
# Extract all WORSE ranks for valid sources for the same variable and species  # Filter for ranks greater than the minimum rank
variable_sources_worse_ranks <- variable_sources_valid_ranks[variable_sources_valid_ranks > min(worth_dataframe$priority[match(variable_sources_valid, worth_dataframe$source)])]
# Get the values for the specified cell and sources
cell_value <- data[species_index, column_index]
variable_values <- data[species_index, variable_colnames]
result <- list(
species = species,
column_name = column_name,
variable = variable,
source = source,
variable_colnames = variable_colnames,
variable_sources_valid = variable_sources_valid,
variable_sources_valid_ranks = variable_sources_valid_ranks,
variable_sources_best_rank = variable_sources_best_rank,
variable_sources_worse_ranks = variable_sources_worse_ranks,
cell_value = cell_value,
variable_values = variable_values
)
return(result)
}
# Example usage:
result <- get_info_for_cell(intermediate_data, species_index = 44, column_index = 258)
print(result)
# ## ## #END SOMETHING
worth_dataframe
View(worth_dataframe)
View(worth_dataframe)
