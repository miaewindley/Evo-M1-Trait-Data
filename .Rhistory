names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
as.data.frame(spreadsheet) <- sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x)))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
data.frame(spreadsheet) <- sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x)))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
data.frame(spreadsheet) <- sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x)))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
colnames(spreadsheet) = gsub("..", ".", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("..", ".", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\..\\", "\\.\\", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\..\\", ".", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\. .\\", ".", colnames(spreadsheet))
View(spreadsheet)
names(spreadsheet)
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\..\\", ".", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", ".", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns
colnames(spreadsheet) = gsub("\\M_\\b", "Macropus_", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns
colnames(spreadsheet) = gsub("\\M_\\", "Macropus_", colnames(spreadsheet))
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
spreadsheet$x106=sum(str_detect(spreadsheet[,2], 'x106'))
library(tidyverse)
spreadsheet$x106=sum(str_detect(spreadsheet[,2], 'x106'))
View(spreadsheet)
spreadsheet$x106=str_detect(spreadsheet[,2], 'x106')
View(spreadsheet)
if (spreadsheet$x106 = TRUE){spreadsheet[,2] = gsub("x106", "", spreadsheet[,2]); df[,2] * 10^6}
spreadsheet$x106
if (spreadsheet$x106 == TRUE){spreadsheet[,2] = gsub("x106", "", spreadsheet[,2]); df[,2] * 10^6}
ifelse (spreadsheet$x106 == TRUE){spreadsheet[,2] = gsub("x106", "", spreadsheet[,2]); df[,2] * 10^6}
ifelse (spreadsheet$x106 == TRUE, spreadsheet[,2] = gsub("x106", "", spreadsheet[,2]) & df[,2] * 10^6,spreadsheet[,2])
ifelse (spreadsheet$x106 == TRUE, spreadsheet[,2] <- gsub("x106", "", spreadsheet[,2]) & df[,2] * 10^6,spreadsheet[,2])
ifelse (spreadsheet$x106 == TRUE, spreadsheet[,2] <- gsub("x106", "") & df[,2] * 10^6,spreadsheet[,2])
getwd()
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns (Genus)
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
# Convert the full number (Steps 1_3)
# Step 1: load tidyverse
library(tidyverse)
# Step 2: look for any given column with a value requiring calculation
for (i in 2:length(names(spreadsheet))) {
spreadsheet$x106=str_detect(spreadsheet[,i], 'x106')
ifelse (spreadsheet$x106 == TRUE, as.numeric(spreadsheet[,i] <- gsub("x106", "", spreadsheet[,i])) & spreadsheet[,i] * 10^6,spreadsheet[,i])
}
View(spreadsheet)
View(spreadsheet)
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns (Genus)
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
# Convert the full number (Steps 1_3)
# Step 1: load tidyverse
library(tidyverse)
# Step 2: look for any given column with a value requiring calculation
for (i in 2:length(names(spreadsheet))) {
spreadsheet$x106=str_detect(spreadsheet[,i], 'x106')
ifelse (spreadsheet$x106 == TRUE, as.numeric(spreadsheet[,i] <- gsub("x106", "", spreadsheet[,i])) & spreadsheet[,i] * 10^6,spreadsheet[,i])
}
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns (Genus)
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
# Convert the full number (Steps 1_3)
# Step 1: load tidyverse
library(tidyverse)
# Step 2: look for any given column with a value requiring calculation
for (i in 2:length(names(spreadsheet))) {
spreadsheet$x106=str_detect(spreadsheet[,i], 'x106')
ifelse (spreadsheet$x106 == TRUE, as.numeric(spreadsheet[,i] <- gsub("x106", "", spreadsheet[,i])) & df[,i] * 10^6,spreadsheet[,i])
}
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns (Genus)
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
# Convert the full number (Steps 1_3)
# Step 1: load tidyverse
library(tidyverse)
# Step 2: look for columns TRUE
# Step 3: If true, equation
for (i in 2:length(names(spreadsheet))) {
spreadsheet$x106=str_detect(spreadsheet[,i], 'x106')
spreadsheet$x109=str_detect(spreadsheet[,i], 'x109')
ifelse (spreadsheet$x106 == TRUE, spreadsheet[,i] <- gsub("x106", "", spreadsheet[,i]), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, spreadsheet[,i] <- gsub("x109", "", spreadsheet[,i]), spreadsheet[,i])
spreadsheet[,i] <- as.numeric(spreadsheet[,i])
ifelse (spreadsheet$x106 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^6), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^9), spreadsheet[,i])
}
# options(scipen=999)
View(spreadsheet)
options(scipen=999)
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns (Genus)
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
# Convert the full number (Steps 1_3)
# Step 1: load tidyverse
library(tidyverse)
# Step 2: look for columns TRUE
# Step 3: If true, equation
for (i in 2:length(names(spreadsheet))) {
spreadsheet$x106=str_detect(spreadsheet[,i], 'x106')
spreadsheet$x109=str_detect(spreadsheet[,i], 'x109')
ifelse (spreadsheet$x106 == TRUE, spreadsheet[,i] <- gsub("x106", "", spreadsheet[,i]), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, spreadsheet[,i] <- gsub("x109", "", spreadsheet[,i]), spreadsheet[,i])
spreadsheet[,i] <- as.numeric(spreadsheet[,i])
ifelse (spreadsheet$x106 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^6), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^9), spreadsheet[,i])
}
options(scipen=999)
View(spreadsheet)
options(scipen=999)
View(spreadsheet)
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns (Genus)
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
# Convert the full number (Steps 1_3)
# Step 1: load tidyverse
library(tidyverse)
# Step 2: look for columns TRUE for 106 or 109. If true, need to multiply equation
for (i in 2:length(names(spreadsheet))) {
spreadsheet$x106=str_detect(spreadsheet[,i], 'x106')
spreadsheet$x109=str_detect(spreadsheet[,i], 'x109')
ifelse (spreadsheet$x106 == TRUE, spreadsheet[,i] <- gsub("x106", "", spreadsheet[,i]), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, spreadsheet[,i] <- gsub("x109", "", spreadsheet[,i]), spreadsheet[,i])
spreadsheet[,i] <- as.numeric(spreadsheet[,i])
ifelse (spreadsheet$x106 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^6), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^9), spreadsheet[,i])
}
spreadsheet$x106 = NULL
spreadsheet$x109 = NULL
# Do this to turn of scientific notation
#options(scipen=999)
View(spreadsheet)
# Read the spreadshsset
spreadsheet<-read.csv("TESTS NOTES cytoarch GLI cell types.csv")
# Remove unwanted names. Takes names from columns 1-7, adds next to them names from 9-last #assigns new names to original df
names(spreadsheet) = c(names(spreadsheet)[1:7],  names(spreadsheet)[9:length(names(spreadsheet))])
# Delete empty column (you need to run it twice or use two similar functions, for some reason)
spreadsheet[,13]<-NULL
spreadsheet=spreadsheet[,-13]
#spreadsheet[,13]=NULL
# rename columns
colnames(spreadsheet)[colnames(spreadsheet) == "X"] ="Anatomy"
colnames(spreadsheet)[colnames(spreadsheet) == "X.2"] ="delta"
# remove unnecessary character in a specific column
spreadsheet$delta = gsub("x", "", spreadsheet$delta)
# remove unnecessary string in the whole dataset
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub("n.a.","",as.character(x))))
spreadsheet <- data.frame(sapply(spreadsheet,function(x) gsub(",","",as.character(x))))
# rename string in columns
colnames(spreadsheet) = gsub("\\b..\\b", "_", colnames(spreadsheet))
# rename string in columns (Genus)
colnames(spreadsheet) = gsub("M_", "Macropus_", colnames(spreadsheet))
# Convert the full number (Steps 1_3)
# Step 1: load tidyverse
library(tidyverse)
# Step 2: look for columns TRUE for 106 or 109. If true, need to multiply equation
for (i in 2:length(names(spreadsheet))) {
spreadsheet$x106=str_detect(spreadsheet[,i], 'x106')
spreadsheet$x109=str_detect(spreadsheet[,i], 'x109')
ifelse (spreadsheet$x106 == TRUE, spreadsheet[,i] <- gsub("x106", "", spreadsheet[,i]), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, spreadsheet[,i] <- gsub("x109", "", spreadsheet[,i]), spreadsheet[,i])
spreadsheet[,i] <- as.numeric(spreadsheet[,i])
ifelse (spreadsheet$x106 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^6), spreadsheet[,i])
ifelse (spreadsheet$x109 == TRUE, as.numeric(spreadsheet[,i] <-  spreadsheet[,i]*10^9), spreadsheet[,i])
}
spreadsheet$x106 = NULL
spreadsheet$x109 = NULL
# Do this to turn of scientific notation
#options(scipen=999)
setwd("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo M1 Trait Data")
list.files("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data")
list.files("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public")
list.files("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public/comparative-data")
dir_path <- "~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public/comparative-data"
file_path <- file.path(dir_path, "file_list.xlsx")
# Get and write the list of files to Excel
write.xlsx(data.frame(Files = list.files(dir_path)), file_path, sheetName = "FileList", colNames = TRUE)
# install.packages("openxlsx")
library(openxlsx)
# Directory and file path
dir_path <- "~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public/comparative-data"
file_path <- file.path(dir_path, "file_list.xlsx")
# Get and write the list of files to Excel
write.xlsx(data.frame(Files = list.files(dir_path)), file_path, sheetName = "FileList", colNames = TRUE)
# install.packages("openxlsx")
library(openxlsx)
# Directory and file path
file_path <- file.path("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public/comparative-data", "file_list.xlsx")
# Get and write the list of files to Excel
write.xlsx(data.frame(Files = list.files("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public/comparative-data")),
"~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/file_list.xlsx", sheetName = "FileList", colNames = TRUE)
# install.packages("openxlsx")
library(openxlsx)
# Directory and file path
file_path <- file.path("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public/comparative-data", "file_list.xlsx")
# Get and write the list of files to Excel
write.xlsx(data.frame(Files = list.files("~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/__Public/comparative-data")),
"~/Library/CloudStorage/OneDrive-AllenInstitute/Species/Evo-M1-Trait-Data/file_list.xlsx", sheetName = "FileList", colNames = TRUE)
